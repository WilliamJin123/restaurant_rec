# restaurant_rec
Restaurant Recommender System

### Ideas

- Association rules for restaurants
- LLM filter / grouper / data enricher (synthetic data gen)
- PowerBI type analytic dashboard
- Standard recommender filter system (tf-idf, reviews, collaborative filtering, content filtering, etc.)
- Standard non-personalized recommendations at first (popular restaurants) --> ask the user to do a bunch of swiping, collect data